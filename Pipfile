[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyspark = "==3.2.2"

[dev-packages]
pytest = "*"
black = "*"

[requires]
python_version = "3.8"

[scripts]
test = "python -m pytest"
format = "black ."
start = "python -m app"
